<h1 align="center">Một số giải pháp monitoring/observability khác ngoài SigNoz</h1>\

**1. Datadog**

Một trong những giải pháp observability nổi tiếng nhất, SigNoz được gọi là một giải pháp open-source alternative của Datadog.

Datadog là một giải pháp giám sát và security cloud-based, dùng để giám sát server, ứng dụng, DB... trong thời gian thực, tổng hợp các log, traces và metrics vào cùng một hệ thống. Datadog sử dụng Datadog agent để gửi 

Gần như đầy đủ mọi tính năng mà một nền tảng observability có thể có:

- Monitor hạ tầng: server, docker container, k8s... 

- Quản lý log.

- Monitor hiệu năng ứng dụng.

- Tăng cường bảo mật cho hạ tầng, ứng dụng.

- Monitor hạ tầng network, giữa các endpoint và host.

- Synthetic monitor: giả lập người dùng và monitor trạng thái.

- Real user monitoring: quan sát mọi hoạt động của một user session.

- Tích hợp AI agent để phân tích các dữ liệu metric, log, trace.

Ưu điểm:

- Các chức năng đều rất mạnh mẽ, cụ thể, chi tiết, phù hợp với thực hiện giám sát quy mô doanh nghiệp lớn.

- Giải pháp có nhiều chức năng nhất.

- Giải pháp all-in-one, mọi chức năng đều do Datadog cung cấp trực tiếp.

Nhược điểm:

- Hoàn toàn cloud-based, dữ liệu sẽ phải chia sẻ với bên thứ 3, ko phù hợp với yêu cầu bảo mật của công ty.

- Sử dụng model thanh toán subscription-based, tính phí hàng tháng riêng cho mỗi host và mỗi chức năng nên sẽ cực kì đắt đỏ.

- Vendor lock-in.

**Kết luận**: Datadog là một giải pháp observability mạnh mẽ nhưng ko phù hợp với những yêu cầu sử dụng của công ty.

**2. Dynatrace**:

Cũng là một giải pháp SaaS cloud-based tương tự như Datadog nhưng nhấn mạnh việc sử dụng AI agent để hỗ trợ các tác vụ observability.

Tương tự như Datadog, các chức năng của Dynatrace đều rất mạnh mẽ nhưng việc phải chia sẽ dữ liệu ra ngoài, kèm với giá thành vô cùng đắt đỏ khiến Dynatrace ko phù hợp với nhu cầu sử dụng của công ty.

**3. OpenObserve, Uptrace...**: Những giải pháp observability open-source self-host all-in-one khác.

Ưu điểm: 

- Có những chức năng cơ bản cần có như quản lý log, trace, metric, monitor hạ tầng, APM, distributed tracing...

- Single binary, dễ triển khai (tương tự như SigNoz)

- Đều tương thích với OpenTelemetry, ko bị vendor lock-in, dễ dàng setup và extract dữ liệu.

Nhược điểm:

- Không có được nhiều tính năng cặn kẽ như SigNoz.

- Không phổ biến như SigNoz, các docs và nguồn hỗ trợ khác không phong phú và chi tiết bằng.

**Kết luận**: Những lựa chọn này có thể đáp ứng được những nhu cầu sử dụng của công ty nhưng không có đặc điểm gì vượt trội hơn so với SigNoz.

**4. Grafana stack**

Stack Grafana là giải pháp observability open-source phổ biến nhất, có cộng đồng sử dụng và hỗ trợ lớn. Stack được cấu thành từ nhiều thành phần khác nhau, mỗi thành phần đảm nhiệm một chức năng khác:

- Metric: Prometheus.

- Logs: Loki.

- Trace: Tempo.

- Dashboard + Alerting: Grafana.

- APM: Tempo + Grafana.

- Real user monitoring: Faro.

- Giả lập user: k6

- Profiling: Pyroscope.

Ưu điểm:

- Không bị vendor lock-in, tương thích với các data source như OpenTelemetry, dễ dàng chuyển đổi sang các option open-source khác nếu cần.

- Cộng đồng hỗ trợ lớn, có rất nhiều dashboard sẵn cho nhiều mục đích khác nhau. Dashboard của Grafana là tiêu chuẩn.

- Có thể tùy chỉnh, thay thế từng thành phần nếu muốn.

Nhược điểm:

- Phải triển khai nhiều thành phần, mỗi thành phần có cấu hình riêng, phức tạp hơn SigNoz chỉ có một binary cần chạy.

- Liên kết metric, log, trace phức tạp hơn, phải tự cấu hình.

**Kết luận** Stack Grafana là một giải pháp hợp lý cho nhu cầu observability của công ty nhưng nếu cần một lựa chọn all-in-one thì stack Grafana ko đáp ứng được.

**5. ELK stack** hay còn gọi là Elastic stack.

ELK stack thường là một stack phần mềm sử dụng để monitor log nhưng giờ đây đã được mở rộng để đảm nhiệm thêm những nhiệm vụ như monitor hạ tầng, APM... Stack gồm 3 phần mềm khác nhau là Elasticsearch, Logstash và Kibana.

- Elasticsearch: engine thực hiện lưu trữ, tìm kiếm và query log.

- Logstash: tiếp nhận log từ nhiều nguồn khác nhau, xử lý và ghi dữ liệu vào Elasticsearch.

- Kibana: giao diện để quản lý thống kê dữ liệu.

- Beats: là các agent gọn nhẹ có nhiệm vụ gửi các dữ liệu về Logstash.

Ưu điểm:

- Tối ưu cho việc log management, là lựa chọn hàng đầu nếu mục tiêu số 1 là log.

Nhược điểm:

- Những chức năng khác chỉ là phụ, thêm vào sau, stack ko phù hợp cho mục đích observability toàn diện.

- Không phải một giải pháp all-in-one, setup phức tạp hơn SigNoz.

<h2>Tổng kết</h2>

SigNoz có thể coi là giải pháp giám sát hạ tầng và ứng dụng open-source, all-in-one mạnh mẽ nhất trên thị trường hiện nay. Những lựa chọn có khả năng giám sát mạnh hơn đều là cloud-based đắt đỏ, còn những lựa chọn mã nguồn mở khác đều không có tính thuận tiện all-in-one của SigNoz hoặc nếu có all-in-one thì ko có điểm gì hơn SigNoz.  

